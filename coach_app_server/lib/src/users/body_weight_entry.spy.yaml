class: BodyWeightEntry
table: body_weight_entry
fields:
  userId: int
  weight: double
  measuredAt: DateTime
indexes:
  body_weight_entry_user_idx:
    fields: userId
  body_weight_entry_measured_at_idx:
    fields: measuredAt

