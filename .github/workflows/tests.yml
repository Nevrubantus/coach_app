name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.8'
      SERVERPOD_PASSWORD_database: '8cQXUfdE4iudDSC3tBtFfICli_9KOdvp'
      SERVERPOD_PASSWORD_redis: '2hZBzQXnubmK559wUA3VI6HAzfTMxfyg'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: coach_app_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: coach_app_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: coach_app_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: coach_app_server
        run: serverpod generate

      - name: Run tests
        working-directory: coach_app_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: coach_app_server
        run: docker compose down -v
